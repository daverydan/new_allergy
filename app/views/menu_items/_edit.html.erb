<div class="modal-wrap">
	<h3>Edit Menu Item</h3>
	<%= form_for @menu_item do |f| %>
		<p>
			<%= f.label :name %>
			<%= f.text_field :name %>
		</p>

		<p><%= f.submit %></p>
		<!-- (@menu_item.new_record? ? "New Menu Item" :  "Edit Menu Item") -->
	<% end %>
</div>